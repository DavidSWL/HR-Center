{
  "name": "swl-hr-platform",
  "private": true,
  "version": "0.0.1",
  "description": "SouthWest Landscape internal HR platform (Firebase).",
  "scripts": {
    "test:rules": "firebase emulators:exec --project=demo-swl-hr --only firestore,storage \"node --test tests/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^15.0.0"
  }
}
